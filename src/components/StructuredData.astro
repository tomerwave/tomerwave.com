---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Tomer Gal",
      url: "https://tomerwave.com/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Tomer Gal",
      logo: {
        "@type": "ImageObject",
        url: "https://tomerwave.com/avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://tomerwave.com/avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Tomer Gal",
    url: "https://tomerwave.com",
    image: "https://tomerwave.com/avatar.jpg",
    sameAs: [
      "https://github.com/tomerwave",
      "https://x.com/TomerWave",
      "https://www.linkedin.com/in/tomergal14",
    ],
    jobTitle: "Software Engineer",
    description:
      "Personal blog and website of Tomer Gal.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Tomer Gal",
    url: "https://tomerwave.com",
    description:
      "Personal blog and website of Tomer Gal.",
    author: {
      "@type": "Person",
      name: "Tomer Gal",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://tomerwave.com/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
